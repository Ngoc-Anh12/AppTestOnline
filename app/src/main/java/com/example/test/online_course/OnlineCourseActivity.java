package com.example.test.online_course;

import androidx.appcompat.app.AppCompatActivity;

import android.annotation.SuppressLint;
import android.content.Context;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;

import com.example.test.R;
//import com.example.test.utils.ZoomManager;

public class OnlineCourseActivity extends AppCompatActivity {
/*EditText txt_id_room, txt_password_room;
Button btn_join;
    ZoomManager zoomManager;
    Context context;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_online_course);
        init();
        handle();

    }
    @SuppressLint("WrongViewCast")
    private void init(){
        txt_id_room = (EditText) findViewById(R.id.txt_id_room);
        txt_password_room = (EditText) findViewById(R.id.txt_password_room);
        btn_join = (Button) findViewById(R.id.btn_join);
        zoomManager = new ZoomManager();
        zoomManager.initializeSdk(OnlineCourseActivity.this);

    }
    private  void handle(){
        btn_join.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                zoomManager.joinMeeting(OnlineCourseActivity.this,"5685177026", "56UMNn","luutb");
            }
        });
    }*/
}